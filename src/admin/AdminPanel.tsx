import { useEffect, useMemo, useState } from 'react';
import type { User } from '@supabase/supabase-js';
import { supabase } from '../lib/supabase';
import { QUESTIONS } from '../data/questions';
import { createAnnouncement, createDpp, getStaffRole, loadAdminAnswers, loadAdminStudents, loadDpps, type AdminAnswer, type AdminStudent, type Dpp, uploadTeacherAsset } from './admin';

type Section = 'overview' | 'students' | 'dpps' | 'questions' | 'announcements';
const subjects = ['Physics', 'Chemistry', 'Biology'] as const;
const icon = (name: string) => ({ overview:'⌂', students:'◉', dpps:'▤', questions:'◇', announcements:'◌' } as Record<string,string>)[name] ?? '•';

export default function AdminPanel({ user, onExit }: { user: User; onExit: () => void }) {
  const [role, setRole] = useState<string | null>(null);
  const [section, setSection] = useState<Section>('overview');
  const [students, setStudents] = useState<AdminStudent[]>([]);
  const [answers, setAnswers] = useState<AdminAnswer[]>([]);
  const [dpps, setDpps] = useState<Dpp[]>([]);
  const [selectedStudent, setSelectedStudent] = useState<AdminStudent | null>(null);
  const [search, setSearch] = useState('');
  const [subject, setSubject] = useState<'All' | typeof subjects[number]>('All');
  const [loading, setLoading] = useState(true);
  const [error, setError] = useState('');
  const [showDpp, setShowDpp] = useState(false);
  const [showAnnouncement, setShowAnnouncement] = useState(false);
  const [notice, setNotice] = useState('');

  useEffect(() => { void boot(); }, []);
  async function boot() {
    setLoading(true); setError('');
    try {
      const r = await getStaffRole(user.id); setRole(r);
      if (!r) return;
      const [s, a, d] = await Promise.all([loadAdminStudents(), loadAdminAnswers(), loadDpps()]);
      setStudents(s); setAnswers(a); setDpps(d);
    } catch (e) { setError(e instanceof Error ? e.message : 'Could not load teacher data.'); }
    finally { setLoading(false); }
  }

  const filteredStudents = useMemo(() => students.filter(s => `${s.display_name ?? ''} ${s.email ?? ''}`.toLowerCase().includes(search.toLowerCase())), [students, search]);
  const stats = useMemo(() => {
    const attempted = answers.length, correct = answers.filter(a => a.is_correct).length;
    const activeIds = new Set(answers.filter(a => Date.now() - new Date(a.answered_at).getTime() < 86400000).map(a => a.user_id));
    const weak = subjects.map(sub => { const x = answers.filter(a => a.subject === sub); return { sub, acc: x.length ? Math.round(x.filter(a => a.is_correct).length / x.length * 100) : 0, n: x.length }; }).sort((a,b) => a.acc-b.acc);
    return { students: students.length, attempted, correct, accuracy: attempted ? Math.round(correct / attempted * 100) : 0, active: activeIds.size, weak };
  }, [students, answers]);

  const studentAnswers = selectedStudent ? answers.filter(a => a.user_id === selectedStudent.id) : [];
  const studentStats = useMemo(() => subjects.map(sub => { const x = studentAnswers.filter(a => a.subject === sub); return { sub, n:x.length, acc:x.length ? Math.round(x.filter(a=>a.is_correct).length/x.length*100):0 }; }), [studentAnswers]);

  if (loading) return <div className="admin-loading"><div className="admin-spinner"/><strong>Opening Teacher Command Center</strong><span>Securing your teaching workspace…</span></div>;
  if (!role) return <div className="admin-denied"><div>403</div><h1>Teacher access required.</h1><p>This account is not assigned a teacher role.</p><button onClick={onExit}>Return to neetprep</button></div>;

  return <div className="admin-app">
    <style>{css}</style>
    <aside className="admin-sidebar">
      <button className="admin-brand" onClick={() => setSection('overview')}><span>N</span><b>neet<span>prep</span></b><small>TEACHER</small></button>
      <div className="admin-nav-label">WORKSPACE</div>
      {(['overview','students','dpps','questions','announcements'] as Section[]).map(x => <button key={x} className={section===x?'active':''} onClick={() => setSection(x)}><i>{icon(x)}</i>{x === 'dpps' ? 'DPPs' : x[0].toUpperCase()+x.slice(1)}{x==='students' && <em>{students.length}</em>}</button>)}
      <div className="admin-sidebar-bottom"><div className="admin-user"><span>{(user.email?.[0] ?? 'T').toUpperCase()}</span><div><b>{user.email?.split('@')[0] ?? 'Teacher'}</b><small>{role}</small></div></div><button onClick={onExit}>← Student app</button></div>
    </aside>
    <main className="admin-main">
      <header className="admin-header"><div><span className="admin-eyebrow">TEACHER COMMAND CENTER</span><h1>{section === 'overview' ? 'Good evening.' : section === 'students' ? 'Your students.' : section === 'dpps' ? 'Daily practice.' : section === 'questions' ? 'Question library.' : 'Announcements.'}</h1><p>{section === 'overview' ? 'One calm view of the whole batch.' : section === 'students' ? 'Inspect progress, accuracy and activity without spreadsheets.' : section === 'dpps' ? 'Build and publish focused practice sets.' : section === 'questions' ? 'Browse, inspect and reuse your question bank.' : 'Keep the whole batch in the loop.'}</p></div><div className="admin-head-actions"><span className="live-chip"><i/> Live data</span><button onClick={() => void boot()}>↻ Refresh</button></div></header>
      {error && <div className="admin-error">{error}</div>}
      {notice && <div className="admin-notice">✓ {notice}</div>}
      {section === 'overview' && <Overview stats={stats} students={students} dpps={dpps} answers={answers} onStudents={() => setSection('students')} onDpp={() => setShowDpp(true)} />}
      {section === 'students' && <Students students={filteredStudents} search={search} setSearch={setSearch} onSelect={setSelectedStudent} answers={answers} />}
      {section === 'dpps' && <Dpps dpps={dpps} onCreate={() => setShowDpp(true)} />}
      {section === 'questions' && <Questions />}
      {section === 'announcements' && <Announcements onCreate={() => setShowAnnouncement(true)} />}
      {selectedStudent && <StudentDrawer student={selectedStudent} answers={studentAnswers} stats={studentStats} onClose={() => setSelectedStudent(null)} />}
      {showDpp && <DppModal onClose={() => setShowDpp(false)} onSaved={async () => { setShowDpp(false); setNotice('DPP published successfully.'); setDpps(await loadDpps()); }} />}
      {showAnnouncement && <AnnouncementModal onClose={() => setShowAnnouncement(false)} onSaved={() => { setShowAnnouncement(false); setNotice('Announcement published.'); }} />}
    </main>
  </div>;
}

function Overview({ stats, students, dpps, answers, onStudents, onDpp }: any) { const latest = [...students].sort((a,b)=>new Date(b.updated_at).getTime()-new Date(a.updated_at).getTime()).slice(0,6); return <>
  <div className="admin-kpis"><Kpi label="Students" value={stats.students} note={`${stats.active} active today`} /><Kpi label="Questions solved" value={stats.attempted.toLocaleString()} note="All recorded answers" /><Kpi label="Batch accuracy" value={`${stats.accuracy}%`} note="Across the whole batch" /><Kpi label="Published DPPs" value={dpps.length} note="Teacher-created sets" /></div>
  <div className="admin-grid-2"><section className="admin-card"><div className="admin-card-head"><div><span>ATTENTION</span><h2>Where the batch needs you.</h2></div><button onClick={onStudents}>View students →</button></div><div className="attention-list">{stats.weak.map((x:any)=><div key={x.sub}><span className={`subject-dot ${x.sub.toLowerCase()}`}>{x.sub[0]}</span><div><b>{x.sub}</b><small>{x.n ? `${x.acc}% accuracy across ${x.n} answers` : 'No answers recorded yet'}</small></div><strong className={x.acc < 60 ? 'bad' : ''}>{x.acc}%</strong></div>)}</div></section>
  <section className="admin-card dpp-launch"><span>TEACHER TOOL</span><h2>Build tomorrow's DPP.</h2><p>Pick questions from the existing bank, set a release time and publish it to your teaching workspace.</p><button onClick={onDpp}>Create DPP <b>＋</b></button></section></div>
  <section className="admin-card"><div className="admin-card-head"><div><span>RECENT STUDENTS</span><h2>Who's moving.</h2></div><button onClick={onStudents}>Open roster →</button></div><div className="student-table">{latest.map((s:any)=>{const a=answers.filter((x:any)=>x.user_id===s.id);const acc=a.length?Math.round(a.filter((x:any)=>x.is_correct).length/a.length*100):0;return <button key={s.id} onClick={onStudents}><span className="student-avatar">{(s.display_name?.[0]??s.email?.[0]??'S').toUpperCase()}</span><span className="student-name"><b>{s.display_name||s.email||'Student'}</b><small>{s.email||'No email on profile'}</small></span><span>{a.length} answers</span><strong>{acc}%</strong><i>›</i></button>})}</div></section>
</>; }
function Kpi({label,value,note}:{label:string,value:any,note:string}){return <div className="admin-kpi"><span>{label}</span><strong>{value}</strong><small>{note}</small></div>}
function Students({students,search,setSearch,onSelect,answers}:any){return <section className="admin-card"><div className="toolbar"><div className="admin-search">⌕<input value={search} onChange={e=>setSearch(e.target.value)} placeholder="Search students…"/></div><span>{students.length} students</span></div><div className="student-table">{students.map((s:any)=>{const a=answers.filter((x:any)=>x.user_id===s.id);const acc=a.length?Math.round(a.filter((x:any)=>x.is_correct).length/a.length*100):0;return <button key={s.id} onClick={()=>onSelect(s)}><span className="student-avatar">{(s.display_name?.[0]??s.email?.[0]??'S').toUpperCase()}</span><span className="student-name"><b>{s.display_name||'Student'}</b><small>{s.email||'Email unavailable'}</small></span><span>{a.length} solved</span><strong className={acc<50?'bad':''}>{acc}%</strong><i>›</i></button>})}</div></section>}
function StudentDrawer({student,answers,stats,onClose}:any){return <div className="drawer-backdrop" onMouseDown={onClose}><aside className="student-drawer" onMouseDown={e=>e.stopPropagation()}><button className="drawer-close" onClick={onClose}>×</button><span className="admin-eyebrow">STUDENT PROFILE</span><div className="drawer-identity"><span>{(student.display_name?.[0]??student.email?.[0]??'S').toUpperCase()}</span><div><h2>{student.display_name||'Student'}</h2><p>{student.email||'No email available'}</p></div></div><div className="drawer-metrics"><Kpi label="Questions" value={answers.length} note="Recorded answers"/><Kpi label="Accuracy" value={`${answers.length?Math.round(answers.filter((x:any)=>x.is_correct).length/answers.length*100):0}%`} note="Overall"/><Kpi label="Target" value={student.target_score} note="NEET score"/></div><h3>Subject performance</h3>{stats.map((s:any)=><div className="drawer-subject" key={s.sub}><span>{s.sub}</span><div><i style={{width:`${s.acc}%`}}/></div><b>{s.acc}%</b><small>{s.n} answered</small></div>)}<h3>Recent answers</h3><div className="mini-answer-list">{answers.slice(0,20).map((a:any)=><div key={a.id}><span className={a.is_correct?'ok':'no'}>{a.is_correct?'✓':'×'}</span><b>{a.question_id.replace('reneet-2026-code60-','Q')}</b><small>{a.subject} · {new Date(a.answered_at).toLocaleDateString()}</small></div>)}</div></aside></div>}
function Dpps({dpps,onCreate}:{dpps:Dpp[];onCreate:()=>void}){return <section className="admin-card"><div className="admin-card-head"><div><span>CONTENT DELIVERY</span><h2>Your DPP library.</h2></div><button className="accent-button" onClick={onCreate}>＋ New DPP</button></div><div className="dpp-list">{dpps.length?dpps.map(d=><div key={d.id}><span className="dpp-icon">D</span><div><b>{d.title}</b><small>{new Date(d.scheduled_for).toLocaleString()} · {d.status}</small></div><strong>Published</strong><i>›</i></div>):<div className="empty-admin">No DPPs yet. Your first one takes about a minute.</div>}</div></section>}
function Questions(){const [term,setTerm]=useState('');const [sub,setSub]=useState('All');const list=QUESTIONS.filter((q:any)=>{const okSub=sub==='All'||q.subject===sub;const okTerm=!term||`${q.question} ${q.chapter} ${q.topic}`.toLowerCase().includes(term.toLowerCase());return okSub&&okTerm;}).slice(0,120);return <section className="admin-card"><div className="toolbar"><div className="admin-search">⌕<input value={term} onChange={e=>setTerm(e.target.value)} placeholder="Search question bank…"/></div><select value={sub} onChange={e=>setSub(e.target.value)}><option>All</option>{subjects.map(s=><option key={s}>{s}</option>)}</select><span>{QUESTIONS.length} total</span></div><div className="question-admin-list">{list.map((q:any,i:number)=><div key={q.id}><span className={`subject-dot ${q.subject.toLowerCase()}`}>{q.subject[0]}</span><div><small>{q.subject} · {q.difficulty} · {q.year}</small><b>{q.question.replace(/\s+/g,' ').slice(0,150)}{q.question.length>150?'…':''}</b></div><button>Use in DPP</button></div>)}</div></section>}
function Announcements({onCreate}:{onCreate:()=>void}){return <section className="admin-card announcement-empty"><span>COMMUNICATION</span><h2>Keep students aligned.</h2><p>Publish a short notice to the batch from one place. Test reminders, DPP releases, schedule changes and revision instructions all belong here.</p><button className="accent-button" onClick={onCreate}>＋ New announcement</button></section>}
function DppModal({onClose,onSaved}:{onClose:()=>void;onSaved:()=>void}){const [title,setTitle]=useState('DPP · Daily Practice');const [description,setDescription]=useState('');const [date,setDate]=useState(new Date(Date.now()+86400000).toISOString().slice(0,16));const [selected,setSelected]=useState<string[]>([]);const [term,setTerm]=useState('');const [file,setFile]=useState<File|null>(null);const [busy,setBusy]=useState(false);const pool=QUESTIONS.filter((q:any)=>!term||q.question.toLowerCase().includes(term.toLowerCase())).slice(0,80);async function save(){setBusy(true);try{const assetPath = file ? await uploadTeacherAsset(file) : undefined; await createDpp({title,description,scheduledFor:new Date(date).toISOString(),questionIds:selected,assetPath});onSaved();}catch(e){alert(e instanceof Error?e.message:'Could not publish DPP.');}finally{setBusy(false)}}return <div className="modal-backdrop"><div className="admin-modal wide"><button className="drawer-close" onClick={onClose}>×</button><span className="admin-eyebrow">DPP BUILDER</span><h2>Build a focused practice set.</h2><div className="form-grid"><label>Title<input value={title} onChange={e=>setTitle(e.target.value)}/></label><label>Release time<input type="datetime-local" value={date} onChange={e=>setDate(e.target.value)}/></label></div><label>Description<textarea value={description} onChange={e=>setDescription(e.target.value)} placeholder="What should students focus on?"/></label><div className="picker-head"><div><span>QUESTIONS</span><b>{selected.length} selected</b></div><div className="admin-search">⌕<input value={term} onChange={e=>setTerm(e.target.value)} placeholder="Find a question…"/></div></div><div className="question-picker">{pool.map((q:any)=><button key={q.id} className={selected.includes(q.id)?'selected':''} onClick={()=>setSelected(p=>p.includes(q.id)?p.filter(x=>x!==q.id):[...p,q.id])}><span>{selected.includes(q.id)?'✓':q.subject[0]}</span><div><small>{q.subject} · {q.difficulty}</small><b>{q.question.replace(/\s+/g,' ').slice(0,105)}…</b></div></button>)}</div><label className="upload-zone">＋ Attach teacher material (optional)<input type="file" accept="image/*,.pdf" onChange={e=>setFile(e.target.files?.[0]??null)}/><small>{file?.name??'PDF, image or supporting material'}</small></label><div className="modal-actions"><button onClick={onClose}>Cancel</button><button className="accent-button" disabled={!selected.length||busy} onClick={()=>void save()}>{busy?'Publishing…':`Publish DPP · ${selected.length}`}</button></div></div></div>}
function AnnouncementModal({onClose,onSaved}:{onClose:()=>void;onSaved:()=>void}){const [title,setTitle]=useState('');const [body,setBody]=useState('');const [busy,setBusy]=useState(false);async function save(){setBusy(true);try{await createAnnouncement(title,body);onSaved();}catch(e){alert(e instanceof Error?e.message:'Could not publish.')}finally{setBusy(false)}}return <div className="modal-backdrop"><div className="admin-modal"><button className="drawer-close" onClick={onClose}>×</button><span className="admin-eyebrow">ANNOUNCEMENT</span><h2>Send a clear message.</h2><label>Title<input value={title} onChange={e=>setTitle(e.target.value)} placeholder="Tomorrow's DPP is live"/></label><label>Message<textarea value={body} onChange={e=>setBody(e.target.value)} placeholder="Write the message students should see…"/></label><div className="modal-actions"><button onClick={onClose}>Cancel</button><button className="accent-button" disabled={!title.trim()||!body.trim()||busy} onClick={()=>void save()}>{busy?'Publishing…':'Publish'}</button></div></div></div>}

const css = `
.admin-app{--a:#58d0c0;--bg:#090b0e;--panel:#11151a;--panel2:#161b21;--line:#252c34;--muted:#89939f;--text:#f2f5f7;min-height:100vh;background:radial-gradient(circle at 70% 0%,rgba(88,208,192,.07),transparent 28%),var(--bg);color:var(--text);display:flex;font-family:Manrope,system-ui,sans-serif}.admin-sidebar{width:240px;position:fixed;inset:0 auto 0 0;border-right:1px solid var(--line);padding:22px 14px;display:flex;flex-direction:column;background:rgba(9,11,14,.78);backdrop-filter:blur(24px);z-index:5}.admin-brand{border:0;background:none;color:var(--text);text-align:left;padding:6px 10px 26px;display:grid;grid-template-columns:38px 1fr;column-gap:10px;align-items:center}.admin-brand>span{grid-row:span 2;width:38px;height:38px;border-radius:13px;background:linear-gradient(145deg,#7be2d4,#2d9e93);display:grid;place-items:center;color:#071012;font-weight:900}.admin-brand b{font-size:19px;letter-spacing:-.05em}.admin-brand b span{color:var(--a)}.admin-brand small{color:var(--muted);font-size:7px;letter-spacing:.2em;font-weight:900}.admin-nav-label,.admin-eyebrow{color:#77818d;font-size:9px;letter-spacing:.18em;font-weight:900}.admin-nav-label{padding:0 11px 8px}.admin-sidebar>button:not(.admin-brand){height:45px;border:0;background:none;color:#8f99a4;border-radius:13px;display:flex;align-items:center;gap:11px;padding:0 12px;font-weight:700;text-align:left;margin:2px 0}.admin-sidebar>button.active{background:rgba(88,208,192,.09);color:#bcebe4}.admin-sidebar>button i{font-style:normal;width:19px;text-align:center;font-size:16px}.admin-sidebar>button em{margin-left:auto;font-style:normal;font-size:9px;background:#20262d;padding:3px 7px;border-radius:99px}.admin-sidebar-bottom{margin-top:auto;border-top:1px solid var(--line);padding-top:14px}.admin-user{display:flex;align-items:center;gap:10px;padding:8px}.admin-user>span{width:33px;height:33px;border-radius:50%;background:#20262d;display:grid;place-items:center;font-size:11px;font-weight:900}.admin-user b,.admin-user small{display:block}.admin-user b{font-size:10px}.admin-user small{color:var(--a);font-size:8px;margin-top:2px}.admin-sidebar-bottom>button{border:0;background:none;color:var(--muted);font-size:10px;padding:12px 8px}.admin-main{margin-left:240px;width:calc(100% - 240px);max-width:1500px;padding:44px 52px 70px}.admin-header{display:flex;justify-content:space-between;gap:30px;align-items:flex-end;margin-bottom:30px}.admin-header h1{font-size:39px;letter-spacing:-.06em;margin:8px 0 5px}.admin-header p{margin:0;color:var(--muted);font-size:12px}.admin-head-actions{display:flex;align-items:center;gap:8px}.admin-head-actions button,.admin-head-actions .live-chip,.admin-card-head button,.toolbar>span{border:1px solid var(--line);background:rgba(255,255,255,.025);color:#aeb7c0;border-radius:11px;padding:10px 13px;font-size:10px;font-weight:800}.live-chip{display:flex;gap:7px;align-items:center}.live-chip i{width:6px;height:6px;border-radius:50%;background:#62d79a;box-shadow:0 0 12px #62d79a}.admin-kpis{display:grid;grid-template-columns:repeat(4,1fr);gap:12px;margin-bottom:14px}.admin-kpi{background:linear-gradient(145deg,rgba(255,255,255,.045),rgba(255,255,255,.018));border:1px solid var(--line);border-radius:19px;padding:20px}.admin-kpi>span,.admin-kpi>small{display:block;color:var(--muted);font-size:9px;font-weight:800}.admin-kpi strong{display:block;font-size:30px;letter-spacing:-.06em;margin:10px 0 4px}.admin-kpi small{font-size:8px}.admin-grid-2{display:grid;grid-template-columns:1.25fr .75fr;gap:14px;margin-bottom:14px}.admin-card{background:rgba(17,21,26,.78);border:1px solid var(--line);border-radius:22px;padding:22px;margin-bottom:14px;box-shadow:0 18px 50px rgba(0,0,0,.13)}.admin-card-head{display:flex;justify-content:space-between;align-items:flex-start;gap:20px;margin-bottom:18px}.admin-card-head span,.dpp-launch>span,.announcement-empty>span{font-size:8px;color:var(--muted);font-weight:900;letter-spacing:.16em}.admin-card-head h2,.dpp-launch h2,.announcement-empty h2{font-size:20px;letter-spacing:-.04em;margin:7px 0 0}.admin-card-head button{background:none;border:0;padding:5px;color:var(--a)}.attention-list>div{display:grid;grid-template-columns:35px 1fr auto;gap:11px;align-items:center;padding:13px 0;border-top:1px solid var(--line)}.subject-dot{width:32px;height:32px;border-radius:11px;display:grid;place-items:center;font-weight:900;background:#20262d;color:#b6c1ca}.subject-dot.physics{color:#79b4ff}.subject-dot.chemistry{color:#70dbca}.subject-dot.biology{color:#76dfa5}.attention-list b,.attention-list small{display:block}.attention-list b{font-size:11px}.attention-list small{font-size:8px;color:var(--muted);margin-top:3px}.attention-list strong{font-size:12px}.attention-list strong.bad,.bad{color:#ef9b9b}.dpp-launch{background:radial-gradient(circle at 90% 20%,rgba(88,208,192,.15),transparent 42%),rgba(17,21,26,.78)}.dpp-launch p{color:var(--muted);font-size:10px;line-height:1.6;max-width:350px;margin:12px 0 25px}.dpp-launch button,.accent-button{border:0;background:var(--a);color:#071012;padding:12px 16px;border-radius:12px;font-weight:900;font-size:10px}.dpp-launch button b{margin-left:8px}.student-table>button{width:100%;display:grid;grid-template-columns:35px minmax(150px,1fr) 120px 55px 18px;align-items:center;gap:13px;border:0;border-top:1px solid var(--line);background:none;color:var(--text);padding:12px 0;text-align:left}.student-avatar{width:31px;height:31px;border-radius:10px;background:#20262d;display:grid;place-items:center;font-size:9px;font-weight:900}.student-name b,.student-name small{display:block}.student-name b{font-size:10px}.student-name small,.student-table>button>span:nth-child(3){color:var(--muted);font-size:8px;margin-top:3px}.student-table>button>strong{font-size:11px}.student-table>button>i{font-style:normal;color:#69737e}.toolbar{display:flex;align-items:center;gap:10px;margin-bottom:14px}.admin-search{height:40px;min-width:270px;display:flex;align-items:center;gap:9px;padding:0 12px;border:1px solid var(--line);background:#0d1115;border-radius:12px;color:#75808b}.admin-search input{border:0;outline:0;background:none;color:var(--text);width:100%;font:inherit;font-size:10px}.toolbar>span{margin-left:auto}.toolbar select,.form-grid input,.admin-modal input,.admin-modal textarea,.admin-modal select{background:#0d1115;color:var(--text);border:1px solid var(--line);border-radius:11px;padding:11px;font:inherit;font-size:10px;outline:none}.question-admin-list>div{display:grid;grid-template-columns:34px 1fr auto;gap:12px;align-items:center;border-top:1px solid var(--line);padding:13px 0}.question-admin-list small,.question-admin-list b{display:block}.question-admin-list small{font-size:8px;color:var(--muted);margin-bottom:4px}.question-admin-list b{font-size:10px;font-weight:700}.question-admin-list button{border:1px solid var(--line);background:none;color:var(--a);border-radius:9px;padding:8px;font-size:8px;font-weight:900}.dpp-list>div{display:grid;grid-template-columns:40px 1fr auto 18px;align-items:center;gap:12px;border-top:1px solid var(--line);padding:14px 0}.dpp-icon{width:35px;height:35px;border-radius:11px;background:rgba(88,208,192,.1);color:var(--a);display:grid;place-items:center;font-weight:900}.dpp-list b,.dpp-list small{display:block}.dpp-list b{font-size:10px}.dpp-list small{color:var(--muted);font-size:8px;margin-top:3px}.dpp-list strong{font-size:8px;color:#6ed59b}.dpp-list i{font-style:normal;color:#69737e}.empty-admin{padding:35px;text-align:center;color:var(--muted);font-size:10px}.announcement-empty{text-align:center;padding:70px 30px}.announcement-empty p{max-width:470px;margin:10px auto 25px;color:var(--muted);font-size:10px;line-height:1.7}.drawer-backdrop,.modal-backdrop{position:fixed;inset:0;background:rgba(0,0,0,.58);backdrop-filter:blur(10px);z-index:30}.student-drawer{position:absolute;right:0;top:0;bottom:0;width:min(500px,100%);background:#0d1115;border-left:1px solid var(--line);padding:34px;overflow:auto;box-shadow:-30px 0 80px rgba(0,0,0,.4)}.drawer-close{position:absolute;right:20px;top:18px;width:34px;height:34px;border:1px solid var(--line);border-radius:10px;background:#151a20;color:#aeb7c0;font-size:20px}.drawer-identity{display:flex;align-items:center;gap:14px;margin:18px 0 26px}.drawer-identity>span{width:54px;height:54px;border-radius:17px;background:rgba(88,208,192,.12);color:var(--a);display:grid;place-items:center;font-size:18px;font-weight:900}.drawer-identity h2{font-size:22px;margin:0}.drawer-identity p{font-size:9px;color:var(--muted);margin:5px 0}.drawer-metrics{display:grid;grid-template-columns:repeat(3,1fr);gap:8px}.drawer-metrics .admin-kpi{padding:13px;border-radius:14px}.drawer-metrics .admin-kpi strong{font-size:20px}.student-drawer h3{font-size:13px;margin:27px 0 13px}.drawer-subject{display:grid;grid-template-columns:75px 1fr 40px;gap:10px;align-items:center;margin:12px 0}.drawer-subject span{font-size:9px}.drawer-subject>div{height:5px;background:#20262d;border-radius:10px;overflow:hidden}.drawer-subject i{display:block;height:100%;background:var(--a);border-radius:10px}.drawer-subject b{font-size:9px;text-align:right}.drawer-subject small{grid-column:2/4;color:var(--muted);font-size:7px}.mini-answer-list>div{display:grid;grid-template-columns:25px 60px 1fr;align-items:center;border-top:1px solid var(--line);padding:9px 0}.mini-answer-list span{width:20px;height:20px;border-radius:7px;display:grid;place-items:center;font-weight:900}.mini-answer-list .ok{background:rgba(98,215,154,.1);color:#62d79a}.mini-answer-list .no{background:rgba(239,155,155,.1);color:#ef9b9b}.mini-answer-list b{font-size:8px}.mini-answer-list small{color:var(--muted);font-size:7px}.admin-modal{position:absolute;left:50%;top:50%;transform:translate(-50%,-50%);width:min(520px,calc(100% - 28px));max-height:90vh;overflow:auto;background:#10151a;border:1px solid var(--line);border-radius:22px;padding:28px;box-shadow:0 30px 100px rgba(0,0,0,.55)}.admin-modal.wide{width:min(760px,calc(100% - 28px))}.admin-modal h2{font-size:25px;letter-spacing:-.05em;margin:8px 0 22px}.admin-modal label{display:block;font-size:9px;font-weight:800;color:#9aa4af;margin:12px 0}.admin-modal input,.admin-modal textarea{width:100%;box-sizing:border-box;margin-top:7px}.admin-modal textarea{min-height:90px;resize:vertical}.form-grid{display:grid;grid-template-columns:1fr 1fr;gap:10px}.picker-head{display:flex;justify-content:space-between;align-items:center;margin:20px 0 10px}.picker-head>div:first-child span,.picker-head>div:first-child b{display:block}.picker-head span{font-size:8px;color:var(--muted)}.picker-head b{font-size:11px;margin-top:4px}.picker-head .admin-search{min-width:220px}.question-picker{height:260px;overflow:auto;border:1px solid var(--line);border-radius:14px}.question-picker button{width:100%;display:grid;grid-template-columns:28px 1fr;text-align:left;gap:10px;border:0;border-bottom:1px solid var(--line);background:none;color:var(--text);padding:10px}.question-picker button>span{width:26px;height:26px;border-radius:8px;background:#20262d;display:grid;place-items:center;font-size:8px;font-weight:900}.question-picker button.selected{background:rgba(88,208,192,.07)}.question-picker button.selected>span{background:var(--a);color:#071012}.question-picker small,.question-picker b{display:block}.question-picker small{font-size:7px;color:var(--muted)}.question-picker b{font-size:8px;margin-top:3px}.upload-zone{border:1px dashed #39434d;padding:15px;border-radius:12px;text-align:center}.upload-zone input{display:none}.upload-zone small{display:block;color:var(--a);margin-top:5px}.modal-actions{display:flex;justify-content:flex-end;gap:8px;margin-top:20px}.modal-actions>button:first-child{border:1px solid var(--line);background:none;color:#aeb7c0;padding:11px 15px;border-radius:11px;font-size:10px;font-weight:800}.admin-loading,.admin-denied{min-height:100vh;background:#090b0e;color:#f2f5f7;display:grid;place-items:center;align-content:center;gap:8px;font-family:Manrope,system-ui}.admin-loading span,.admin-denied p{color:#89939f;font-size:11px}.admin-spinner{width:24px;height:24px;border:2px solid #263038;border-top-color:#58d0c0;border-radius:50%;animation:spin .8s linear infinite}.admin-denied>div{font-size:60px;font-weight:900;color:#58d0c0}.admin-denied button{border:0;background:#58d0c0;padding:11px 16px;border-radius:11px;font-weight:900}.admin-error,.admin-notice{padding:11px 14px;border-radius:12px;margin-bottom:14px;font-size:9px}.admin-error{background:rgba(239,155,155,.08);color:#ef9b9b;border:1px solid rgba(239,155,155,.2)}.admin-notice{background:rgba(98,215,154,.08);color:#8be0ad;border:1px solid rgba(98,215,154,.18)}@keyframes spin{to{transform:rotate(360deg)}}
@media(max-width:850px){.admin-sidebar{width:72px;padding:14px 8px}.admin-brand{grid-template-columns:1fr;padding:5px 8px 20px}.admin-brand>span{grid-row:auto}.admin-brand b,.admin-brand small,.admin-sidebar .admin-nav-label,.admin-sidebar>button:not(.admin-brand){font-size:0}.admin-sidebar>button:not(.admin-brand){justify-content:center;padding:0}.admin-sidebar>button i{font-size:17px}.admin-sidebar>button em{display:none}.admin-user div,.admin-sidebar-bottom>button{display:none}.admin-user{justify-content:center}.admin-main{margin-left:72px;width:calc(100% - 72px);padding:28px 20px 50px}.admin-header{align-items:flex-start}.admin-header h1{font-size:31px}.admin-kpis{grid-template-columns:1fr 1fr}.admin-grid-2{grid-template-columns:1fr}.student-table>button{grid-template-columns:32px 1fr 50px 18px}.student-table>button>span:nth-child(3){display:none}.admin-head-actions .live-chip{display:none}}
@media(max-width:560px){.admin-main{padding:20px 13px}.admin-header{display:block}.admin-header h1{font-size:28px}.admin-head-actions{margin-top:15px}.admin-kpis{gap:7px}.admin-kpi{padding:15px}.admin-kpi strong{font-size:24px}.admin-card{padding:16px;border-radius:18px}.toolbar{display:block}.toolbar .admin-search{min-width:0;margin-bottom:8px}.form-grid{grid-template-columns:1fr}.picker-head{display:block}.picker-head .admin-search{margin-top:10px;min-width:0}.student-drawer{padding:25px 18px}.drawer-metrics{grid-template-columns:1fr 1fr}.drawer-metrics .admin-kpi:last-child{grid-column:span 2}.admin-modal{padding:22px}.admin-modal h2{font-size:21px}}
`;
